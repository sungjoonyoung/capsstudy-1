/*
#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>



int GCDP(int, int);

int dis(int);

int main(void) {
	int a;
	int b;
	int LCM;
	printf("두 수를 입력하세요 : ");
	scanf("%d %d", &a, &b);
	int k = GCDP(a, b);
	printf("%d", k);


}
int GCD = 1;
int GCDP(x, y) {
	xs = x;
	ys = y;
	int prime = 2;
	while (x%prime == 0) {
		if (y % prime == 0) {
			GCD = GCD * prime;
		}
		prime = prime + 1;
		
	}
	GCDP(x / prime, y / prime);
	if (xs < prime) {
		if (ys < prime) {
			return GCD;
		}
	}
	else if (ys)
	
}
*/